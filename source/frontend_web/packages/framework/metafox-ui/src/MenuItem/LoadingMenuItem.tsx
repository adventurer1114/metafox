/**
 * @type: ui
 * name: menuItem.as.loading
 */
import { CircularProgress } from '@mui/material';
import React from 'react';

export default function LoadingMenuItem() {
  return <CircularProgress />;
}
